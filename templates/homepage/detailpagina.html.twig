{% extends 'base.html.twig' %}

{% block title %}Hello HomepageController!{% endblock %}

{% block body %}
    <a href="{{ path('saveSollicitatie', {'id':data.id} ) }}">Solliciteer!</a>
    <h1>Detailpagina</h1>
    <div>Titel: {{ data.titel }}</div>
    <div>Niveau: {{ data.niveau.tekstveld }}</div>
    <div>Platform: {{ data.platform.tekstveld }}</div>
    <div>Bedrijf: {{ data.bedrijf.naam }}</div>
    <div>Vacature: <p>{{ data.vacatureBeschrijving | raw }}</p></div>

{% endblock %}